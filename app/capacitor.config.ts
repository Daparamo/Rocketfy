import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Rocketfy',
  appName: 'Rocketfy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
